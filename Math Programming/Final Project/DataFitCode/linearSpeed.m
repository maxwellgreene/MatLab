% Jason M Graham
% 03/14/18
function y = linearSpeed(t,a,b)
  % Function that inputs values for
  % t - independent variable
  % a,b - parameters
  % and returns y according to y = a*t + b
  % Note that t can be a vector but a,b must be scalars. 
  
  y = a*t + b;
  
end